#!/usr/bin/env python
# url arguments pretty-printer

import sys, urlparse, cgi, pprint

args = {}
url = sys.argv[1]
for name, val in cgi.parse_qsl(urlparse.urlparse(url)[4]):
    args.setdefault(name, []).append(val)
pprint.pprint(args)
